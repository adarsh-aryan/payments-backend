services:
  - type: web
    name: payments-recon
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /transactions
    # For SQLite demo; set DATABASE_URL for managed DBs
    envVars:
      - key: DATABASE_URL
        value: "sqlite:////app/data.db"
